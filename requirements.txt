# WorldOfTaxonomy dependencies

# Phase 1: Data Model & Ingestion
openpyxl>=3.1.0
xlrd>=2.0.0
pyarrow>=14.0.0
asyncpg>=0.29.0
python-dotenv>=1.0.0

# Phase 2: REST API
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# Phase 3: MCP Server
# No external dependency - protocol implemented directly (Python 3.9 compatible)

# Phase 4: Web Frontend
jinja2>=3.1.0

# Phase 5: Auth & Rate Limiting
bcrypt>=4.0.0
PyJWT>=2.8.0
slowapi>=0.1.9
httpx>=0.27.0

# Phase 6: AI Taxonomy Generation and classify fallback
# All LLM calls go through world_of_taxonomy.llm_client against Ollama Cloud
# (OpenAI-compatible endpoint at https://ollama.com/v1). No vendor SDK needed;
# httpx (installed above) is the only runtime dependency.

# Phase 7: Observability (optional - only active when SENTRY_DSN is set)
sentry-sdk[fastapi]>=2.0.0

# Prometheus metrics exposition.
prometheus-client>=0.20.0

# Phase 8: Schema migrations
# alembic ships with SQLAlchemy; we only use it for DDL migrations, not as
# an ORM. Keeps init_db()/schema.sql working for fresh installs; alembic
# is the path for incremental DDL from now on.
alembic>=1.13.0
SQLAlchemy>=2.0.0
psycopg[binary]>=3.1.0

# Phase 9: Additional ingesters
# ISO 3166-2 subdivision ingester uses pycountry, which wraps the official
# ISO 3166-2 data tables (no upstream HTTP call required).
pycountry>=23.12.11

ijson>=3.2.0
